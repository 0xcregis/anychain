[package]
name = "anychain-bitcoin"
description = "A Rust library for Bitcoin-focused cryptocurrency wallets, enabling seamless transactions on the Bitcoin blockchain"
authors = ["Cregis.com"]
version = "0.1.0"
license = "MIT"
homepage = "https://www.cregis.com/"
repository = "https://github.com/cregis-web3/anychain"
keywords = ["bitcoin", "blockchain", "cryptocurrency", "wallet", "transactions", "rust"]
edition = "2021"

[dependencies]
anychain-core = { path = "../anychain-core", version = "0.0.1"}
base58 = { version = "0.2" }
bech32 = { version = "0.9.0" }
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"] }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }
sha2 = { version = "0.10.2", default-features = false }
rand = { version = "0.8.5"}
hex = "0.4"
thiserror = "1.0"

[features]
default = ["std"]
std = ["anychain-core/std"]
